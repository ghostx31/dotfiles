[Trigger]
Operation = Install
Type = Package 
Target = linux-lts 
Target = linux-zen 

[Action]
Description = Don't forget to sign new kernels using sbctl 
When = PostTransaction 
Exec = /usr/bin/notify-send "Run sbctl sign --save /boot/newkernel"

